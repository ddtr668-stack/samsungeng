# 대시보드 v3 · 변경사항

> 기간: 2026-09-25 · 사이트: https://ddtr668-stack.github.io/samsungeng/
> 각 버전 옆 `#번호` 는 GitHub PR 번호입니다 (https://github.com/ddtr668-stack/samsungeng/pull/번호).

## 한눈에 보기

| 버전 | 주요 내용 | PR | Apps Script 재배포 |
|---|---|---|---|
| v3.0 | 계약관리 v3 업로드 · 첫 접속 오류 수정 | #1–#2 | – |
| v3.1 | 로그인 · 설정 공용 저장 · 서버 주소 등록 | #3–#7 | 필요 (`-01`, `-02`) |
| v3.2 | 사용자·권한·담당자 · 회원가입 | #8 | 필요 (`-03`) |
| v3.3 | 계약 상세 Google 로그인 창 자동 팝업 제거 | #9 | – |
| v3.4 | 담당자별 계약번호 (이상규-001) | #10 | 필요 (`-04`) |
| v3.5 | 대시보드 담당자·기간 선택, 차트 탭·엑셀 내보내기 | #11–#12 | – |
| v3.6 | 공사 진행 일괄 저장·날짜, 도급비 지급완료, 다른 계약 이동 | #13–#15 | 필요 (`-05`) |
| v3.7 | 담당자별 보기(전 화면) · 리포트 · 자동 로그아웃 | #16–#21 | – |
| v3.8 | 지출품의서 (여러 프로젝트 한 번에) | #22–#24 | – |
| v3.9 | 로그인 안내 개선 · 설정값 유지 버그 수정 | #25–#26 | – |

**현재 Apps Script 최신 배포 버전: `2026-09-25-05`** (설정 → 연결 테스트 또는 로그인 실패 화면에 표시)

---

## v3.0 · 업로드 · 첫 접속 (#1, #2)

- 계약관리 v3 전체 파일 업로드, GitHub Pages 로 배포
- **실데이터 보호**: 저장소가 공개(Public)이므로 거래처·금액이 담긴 `assets/data/app-data.json` 은 올리지 않음 (`.gitignore`)
- 파일명이 깨진 중복 `진단.html`, 빈 패치 폴더 제외
- 서버 미연결 + 샘플 데이터 없음 상태에서 "데이터 로드 실패"로 멈추던 문제 → 설정 화면으로 안내

## v3.1 · 로그인 · 설정 공용 저장 (#3–#7)

- **로그인 화면** 추가, 로그인 30일 유지, 사이드바 하단 로그아웃
- Apps Script `Auth.gs` 신규: 비밀번호 해시 저장, 로그인 토큰, 15분 10회 실패 잠금
- **설정 공용 저장**: 스프레드시트 이름·URL, Google Drive 키를 서버에 저장 → 다른 브라우저에서도 동일
- 관리자 비밀번호 변경 (설정 화면)
- 사이드바 "데이터 소스" → 설정 화면 "데이터 소스 · 연결된 스프레드시트"로 이동
- 다른 .gs 파일과 이름 충돌 방지: 로그인 관련 함수·저장 키 모두 `SG_` 접두사
- 로그인 실패 시 서버 배포 버전 표시, 점검 함수 `SG_checkLogin`
- 로그인 화면 **"서버 주소 변경"** (예전 주소가 남아 있어도 새 주소 입력 가능)
- 서버 주소를 `assets/config.js` 에 등록 → 어느 브라우저든 주소 입력 없이 로그인 화면

## v3.2 · 사용자 · 권한 · 담당자 (#8)

- **회원가입**: 아이디 · 비밀번호 · 이름 · 전화번호 · 부서명 → 관리자 승인 후 로그인
- **권한**: 관리자(전체 + 사용자·설정) / 편집(조회 + 등록·수정) / 조회(보기만) + **본인 담당만** 옵션
  - 권한은 Apps Script 가 요청마다 확인 → 변경·사용 중지 즉시 반영
- **설정 → 사용자·담당자 관리**: 승인·거절, 권한 변경, 본인 담당만, 사용 중지, 비밀번호 초기화, 삭제 (마지막 관리자는 강등·중지 불가)
- **계약 담당자**: `계약담당자` 시트 자동 생성, 기존 계약은 모두 **이상규**
  - 계약 목록 담당자 열, 계약 상세에서 관리자가 담당자 변경, 신규 계약은 등록자가 담당자
- 사이드바에 로그인한 사람 이름·부서·권한 표시

## v3.3 · Google 로그인 창 자동 팝업 제거 (#9)

- 계약 상세를 열 때마다 뜨던 Google 계정 선택 창 제거
- 현장 자료 폴더 카드에 **[Google Drive 연결]** 버튼, 연결 후 약 1시간 유지

## v3.4 · 담당자별 계약번호 (#10)

- 계약번호를 **`이상규-001`** 형식으로 표시 (기존 계약은 계약번호 순으로 이상규-001 ~)
- 신규 계약·담당자 변경 시 해당 담당자의 다음 번호
- 내부 전체 번호(#0106)는 유지 → 수금·지출 기록 영향 없음 (상세 화면에 함께 표시)
- 목록·상세·대시보드·지출·모달·지출품의서 출력물에 적용, 검색 가능

## v3.5 · 대시보드 (#11, #12)

- **담당자 선택** → 해당 담당자 계약만으로 KPI·차트·표 재계산
- **기간 선택** (전체·올해·작년·최근 6/12개월 + 시작/끝 월 직접 선택)
- 월별 추이 차트 **6M / 12M / 전체** 탭 동작
- **엑셀 내보내기**: 현재 조건의 계약 목록 CSV
- 고정값이던 기준일·총 건수·신규 건수를 실제 데이터로, "완공 예정(D-30)" → "미수 잔금 계약"

## v3.6 · 계약 상세 · 지출·기성 (#13–#15)

- **공사 진행 단계**: 여러 단계를 선택한 뒤 **[저장]** 한 번으로 저장 (저장 안 된 변경 표시·취소·이탈 확인)
- **공사 진행 날짜**: 완료한 단계마다 날짜 선택, 비워두면 저장한 날짜로 기록 → `공사진행일자` 시트, 진행 타임라인에 착공·시운전·준공 날짜 표시
- **설치비(도급비) [지급완료]** 버튼: 전액 지급 처리 → 지출·기성 관리 "정산완료", 되돌리기 가능
- 계약 상세 "OO의 다른 계약" 카드 클릭 → 해당 계약으로 이동
- 지출·기성 관리 목록의 **상세 · 기성** 버튼 연결 (동작하지 않던 버튼)

## v3.7 · 담당자별 보기 · 리포트 · 자동 로그아웃 (#16–#21)

- **리포트·통계**: 담당자 선택, PDF 출력, 엑셀 리포트(보고 있는 탭 집계표)
- **10분 무사용 자동 로그아웃**: 1분 전 경고 + [계속 사용], 창을 닫았다 10분 뒤 열어도 로그아웃
  - 자동 로그아웃 뒤 재로그인이 안 되던 문제 수정 (#19)
- **담당자별 보기 (관리자 전용)**
  - 사이드바 "담당자" 목록 (담당자별 계약 건수) → 대시보드·계약·미수금·거래처·지출·리포트 모두 전환
  - 각 화면 상단 담당자 선택 · 상단 "OOO 담당 보기 ✕"
  - 가입·승인된 담당자는 계약 0건이어도 목록에 표시
  - 관리자가 아니면 목록·선택 칸 숨김 (본인 계약만 보게 하려면 "본인 담당만" 권한 사용)
- 계약 관리 **엑셀 내보내기**, 미수금 **수금 리포트**, 거래처 **거래처 CSV**, 지출 **지출 리포트** 버튼 연결

## v3.8 · 지출품의서 (여러 프로젝트) (#22–#24)

- 지출·기성 관리 화면 상단 버튼 **"＋ 지출품의서"** (다른 화면은 "신규 계약")
- 기성 잔액이 남은 프로젝트를 여러 건 선택, 금회 청구액 기본값 = 기성 잔액 (수정 가능)
- **출력물 (기존 지출품의서와 같은 양식)**
  - 첫 장: 품의제목·문서번호·품의일·작성자·지급 예정일·금회 요청금액(한글)
  - 1. **손익계산서**: 프로젝트마다 1줄 (총계약금·제품대·설치비·기타비용·영업수수료·손익·수익률)
  - 2. **금회 지급 내역**: 도급금액·기지급·금회 지급·잔액·입금 계좌
  - 첨부: 프로젝트별 **세부 내역** 페이지 (손익표 + 제품대·설치비·기타비용·영업수수료 품목 — 최근 저장된 지출품의서에서 불러옴)
  - 결재란 없음 (#24)
- **[기성 완료액에 반영]**: 선택한 계약의 도급 지급액에 금회 청구액을 더함

## v3.9 · 안정화 (#25, #26)

- 로그인 실패 시 웹앱이 예전 버전이면 재배포 방법을 바로 안내
- **Google Drive 키 등 설정이 지워지던 문제 수정**: 저장 시 해당 항목만 전송, 서버 값이 비어도 마지막 저장값 유지, [삭제]만 모든 브라우저에서 삭제

---

## Apps Script (구글 쪽 서버) 관리

### 파일
| 파일 | 역할 |
|---|---|
| `gas-backend/DashboardApi.gs` | 데이터 API · 공사 진행 날짜 |
| `gas-backend/Auth.gs` | 로그인 · 사용자·권한 · 담당자 · 공용 설정 |

### 코드를 바꾼 뒤 반영 방법
1. 두 파일을 저장소 최신 내용으로 교체 → 저장
2. **배포 → 배포 관리 → ✏️ 연필 → 버전 "새 버전" → 배포**
   - ⚠️ "새 배포"는 주소가 다른 웹앱을 새로 만듦 → 사이트가 계속 예전 웹앱에 접속
3. 설정 → 연결 테스트에서 배포 버전 확인

### 편집기에서 실행하는 함수
| 함수 | 용도 |
|---|---|
| `SG_setAdminPassword` | 관리자 비밀번호 설정 (NEW_PASSWORD 입력 → 실행 → 다시 지우기) |
| `SG_checkLogin` | 사용자 목록·비밀번호 일치 확인, 로그인 실패 잠금 해제 |

### 자동으로 생기는 시트
| 시트 | 내용 |
|---|---|
| `계약담당자` | 계약번호 · 담당자 · 담당자번호 |
| `공사진행일자` | 계약번호 · 배관 · 실내기 · 실외기 · 시운전 · 인수인계 날짜 |

### 스크립트 속성 (프로젝트 설정 → 스크립트 속성)
| 키 | 내용 |
|---|---|
| `SG_USERS` | 사용자 목록 (비밀번호는 해시로만 저장) |
| `SG_SESS_…` | 로그인 세션 (30일) |
| `SG_APP_SETTINGS` | 공용 설정 (스프레드시트·Drive 키) |

---

## 참고 · 남은 작업

- 동작하지 않는 버튼: 미수금 **독촉장 발송** (발송 방식 결정 필요)
- 계약번호(NO)가 겹치면 같은 번호 계약은 담당자·수정이 함께 적용됨 → 번호가 겹치지 않게 관리
- 여러 프로젝트 지출품의서의 세부 내역은 **최근 저장된 지출품의서** 기준 → 계약관리 금액과 다를 수 있음
- 공개 저장소이므로 실데이터 파일(`app-data.json`, 계약관리 엑셀)은 올리지 않음
