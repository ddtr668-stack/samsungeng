# 대시보드 v2 · 변경사항

## 🆕 이번 업데이트 내용 (2026-09-21)

### 1. 💰 수금 누적 관리 (신규)
- 계약 상세 → 수금액 KPI 카드 → **[＄ 관리]** 초록 버튼 클릭
- 회차별 수금 등록 (수금일 · 금액 · 결제 수단 · 비고)
- 진행 바 시각화 (총계약금 대비 %)
- 합계는 계약관리 시트의 `paidAmount` 로 자동 동기화

### 2. 🧾 지출품의서 대개편 (8가지 개선안)
| # | 개선 |
|---|------|
| ① | 전회 기성 드롭박스 (이전 회차 자동 복원) |
| ② | 기성 누적 % (총 도급금액 대비) |
| ③ | 기타경비 / 영업수수료 항목별 관리 |
| ④ | 도급업체 정보 블록 (거래처 시트에서 7 필드 자동 채움) |
| ⑤ | 비고 사항 (자유 입력) |
| ⑥ | 제품 내역서 (장비대) 임포트 |
| ⑦ | 계약 요약 헤더 (KPI 8개 + 진행 타임라인) |
| ⑧ | PDF 다중 페이지 (1p 표지 + 2~4p 상세) |

**액션 버튼 3분리:**
- **💾 저장** — 지출품의서이력 시트에 기록만
- **🖨️ 출력** — 브라우저 인쇄 (다중 페이지 A4)
- **📄 PDF 다운로드** — GAS로 PDF 변환 + 자동 다운로드 + Drive 저장

### 3. 📁 Drive 저장소 확장
- 하위 카테고리 폴더 **5개 → 7개**
- 신규: `지출품의서/` (회차별 PDF), `증빙/` (회차별 서브폴더 · 영수증)
- 저장 시 Drive에 자동 업로드 (자격 증명 등록 시)

---

## 🔧 GAS 재배포 필요

이번 버전은 `DashboardApi.gs` 에 **7개 신규 API 라우트** 를 추가했습니다:

| 라우트 | 용도 |
|--------|------|
| `payments` | 수금 회차 목록 조회 |
| `createPayment` | 수금 회차 등록 |
| `updatePayment` | 수금 회차 수정 |
| `deletePayment` | 수금 회차 삭제 |
| `expenseByContract` | 특정 계약의 지출품의서 이력 (드롭박스용) |
| `saveExpense` | 지출품의서 저장 (PDF 없이 이력만) |

### 재배포 절차
1. Apps Script 편집기 → `DashboardApi.gs` 파일을 배포판의 것으로 교체
2. **저장** → **배포 → 배포 관리 → ✏️ → 새 버전 → 배포**
3. 웹 앱 URL은 그대로 유지됨 → 대시보드 설정 재입력 불필요

### 자동 생성되는 시트
GAS 재배포 후 수금 관리 기능을 사용하면 다음 시트가 **자동 생성** 됩니다:

- `수금이력` 시트 (컬럼: no · contractNo · roundNo · paymentDate · amount · method · note · createdAt · createdBy)

기존 `계약관리` · `거래처관리` · `지출품의서이력` 시트는 그대로 사용됩니다.

---

## 📦 새 파일 목록 (v2)

### 프론트
```
assets/js/
├── modal-payment.jsx          🆕 수금 누적 관리 모달
├── modal-expense.jsx          🔄 대개편 (v1 → v2)
├── modal-expense-parts.jsx    🆕 재사용 컴포넌트 5종
├── modal-expense-pdf.jsx      🆕 PDF 다중 페이지 빌더
├── lib-drive-folders.jsx      🔄 지출품의서·증빙 카테고리 추가 + 업로드 함수
├── api-client.jsx             🔄 신규 API 6개 추가
└── screens-detail.jsx         🔄 수금 관리 모달 마운트
```

### 백엔드
```
gas-backend/
├── DashboardApi.gs            🔄 신규 라우트 7개 추가 (수금 + 지출품의서 확장)
├── ExpenseRequest.gs          (기존 · 재배포만 필요)
└── PATCH_EDITABLE_FIELDS.gs   (기존)
```

---

## 🎯 사용 순서

### A. 수금 관리 사용
1. 계약 상세 페이지 진입
2. 상단 KPI '수금액' 카드 → [＄ 관리] 버튼 클릭
3. [+ 수금 추가] → 회차 입력 → 저장

### B. 지출품의서 (개편판)
1. 계약 상세 → [지출품의서 생성] 버튼
2. 상단에서 도급업체 선택 (거래처관리 시트에서 자동)
3. 지급 요청 정보 입력 (금액 · 회차 · 비고)
4. 필요 시 설치비 · 제품 · 기타경비 · 수수료 항목 추가
5. 하단 세 버튼 중 선택:
   - **저장** → 이력 시트에 기록만
   - **출력** → 인쇄 (다중 페이지)
   - **PDF 다운로드** → 파일 저장 + Drive 자동 업로드

### C. Drive 자동 저장 (선택)
1. 설정 → Google Drive API 인증 정보 등록
2. 계약 상세 → 현장 자료 폴더 카드에서 7 카테고리 실시간 표시
3. 지출품의서 PDF는 `지출품의서/` 폴더에 자동 저장
4. 증빙 영수증은 `증빙/N차_YYYY-MM-DD/` 서브폴더에 자동 저장

---

## 🆕 v2.1 · 지출품의서 출력·입력 개선 (2026-09-21)

**출력 (A4 세로 전용 문서)**
- `🖨️ 출력` / `👁 출력 미리보기` 가 A4 세로 전용 문서로 출력됩니다. (다른 화면의 가로 인쇄에는 영향 없음)
- **손익 계산** 추가 — 총 계약금액 − 설치비 − 제품 내역서 − 기타 경비 − 영업 수수료 = 예상 손익 · 수익률
- **금회 지급 총액 표 삭제** (상단 요청금액 박스와 중복)
- **계약금액 · 수금 내역** 추가 — 대시보드 수금이력 연동
  - 1회차 입금 = 계약금 / 이후 입금 중 잔금이 남아 있으면 모두 합산 = 중도금 / 잔금을 채우는 입금 = 잔금(미수면 미수 표시)
- **"기성 진행 현황" → "설치비 기성"** 으로 명칭 변경 (입력 화면 · 출력물)
- 도급업체 표에 **대표자** 추가
- 하단 회사명은 **작성자가 선택/직접 입력** (기본 주식회사 삼성이엔지 · 한별상회 선택 가능 · 마지막 선택 기억)

**입력 화면**
- 상단 헤더(지출품의서 · 품의제목 · 요청금액)를 아래 카드와 같은 스타일로 통일
- 계약 요약 아래에 **계약금 / 중도금 / 잔금 / 수금 합계** 타일 추가
- 도급업체 선택: **대표자 칸 추가**(업체명 ↔ 사업자번호 사이), 목록을 *등록 도급업체 → 계약에 입력된 도급업체 → 그 밖의 거래처* 로 확장
  - 업체를 바꾸면 이전 업체의 담당자·연락처·계좌가 남지 않고 새 업체 값으로 교체 (계좌 오입금 방지)
  - 계약의 도급업체가 등록업체와 같으면 열 때 자동으로 정보 채움

**파일**
- 신규: `assets/js/expense-print.jsx`
- 수정: `assets/js/modal-expense.jsx`, `assets/js/modal-expense-parts.jsx`, `index.html`
- 수정(선택 · Apps Script 재배포 필요): `gas-backend/DashboardApi.gs` — 구글 시트의 `도급업체관리` / `도급업체` / `도급업체 대장` / `설치팀` 시트를 읽어 도급업체 목록에 추가 (1행 헤더: 사업자등록번호 · 상호 · 대표자 · 전화번호 · 담당자 · 주소 · 은행 · 계좌번호 · 예금주). 시트가 없으면 기존처럼 동작합니다.
